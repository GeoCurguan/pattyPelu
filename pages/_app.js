import "../styles/style.css";
import '../styles/Calendar.css';

const App = ({ Component, pageProps }) => {
    return (
        <>
            <Component {...pageProps} />
        </>
    );
};
export default App;